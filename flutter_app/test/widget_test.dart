import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App loads without errors', (WidgetTester tester) async {
    // Basic test placeholder - app requires async initialization
    expect(true, isTrue);
  });
}
